# Admin panel env — copy to .env.local
# API base (Go server). Local dev default below.
NEXT_PUBLIC_API_BASE=http://localhost:9080
# Public site URL (used for login redirect + links)
NEXT_PUBLIC_SITE_URL=https://portshare.kexoz.dev
