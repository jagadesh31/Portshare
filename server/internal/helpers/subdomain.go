package helpers

// /subdomain is the username of the user, we will check if the subdomain exists in our database and if it does, we will return return.
func checkSubdomainExists(subdomain string) bool {
	// Implementation for checking if a subdomain exists
	return false
}
